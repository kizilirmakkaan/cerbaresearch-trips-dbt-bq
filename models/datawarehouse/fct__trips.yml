version: 2

models:
  - name: fct__trips
    description: "{{ doc('fct__trips') }}"
    columns:
      - name: _partition_date
        description: "{{ doc('_p_date') }}"
      - name: total_cost_to_passenger
        description: "{{ doc('total_cost_to_passenger') }}"